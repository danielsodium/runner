# Simple Bash Runner

I needed a simple bash runner for work, and couldn't use make or just because of circumstances. This takes yaml tree in ~/.config/runner/config.yaml and constructs a bash command given arguments to run and runs it.
